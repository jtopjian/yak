task::state:
  steps:
    - name: install memcached
      action: exec
      input:
        cmd: apt-get install -y memcached
    - name: install memcached
      action: exec
      input:
        cmd: apt-get install -y memcached
